var num1 = Number(prompt("Enter the first number"))
var num2 = Number(prompt("Enter the first number"))
if (num1>num2) {index=num2
    while (num1>=index) {console.log(index)
        index++;
        
    }
    
} else {index=num1 
    while (num2>=index) {console.log(index)
    index++;
    
}
    
}
